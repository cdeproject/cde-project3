---
- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Copy backend page
  template:
    src: "files/artifact.tar.gz"
    dest: "~/web/artifact.tar.gz"

- name: Executing node
  shell: |
    cd ~/web
    pm2 stop default
    pm2 start npm -- start